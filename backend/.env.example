# PostgreSQL Configuration (for production / PostgreSQL):
# DATABASE_URL="postgresql://postgres:password@localhost:5432/buyora_db?schema=public"

# SQLite Configuration (default zero-config out-of-the-box):
DATABASE_URL="file:./dev.db"

JWT_SECRET="your_jwt_secret_key_here"
PORT=5000
GEMINI_API_KEY="your_gemini_api_key_here"
NODE_ENV=development
